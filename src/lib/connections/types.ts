export interface Connection {
  // id: string;
  // displayName?: string;
  // domain: string;
  url: string;
  client_name: string;
  // lastConnected: string;
  logo_uri?: string;
  verified: boolean;
  first_interacted: string;
  last_interacted: string;
}

export interface Connection {
  // id: string;
  // displayName?: string;
  // domain: string;
  url: string;
  // lastConnected: string;
  logo_uri?: string;
  verified: boolean;
  first_connected: string;
  last_connected: string;
}

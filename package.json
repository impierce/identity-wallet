{
  "workspaces": [
    "unime"
  ],
  "scripts": {
    "build": "npm run -w unime build",
    "check": "npm run -w unime check",
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "lint": "npm run -w unime lint",
    "tauri": "npm run -w unime tauri",
    "test": "npm run -w unime test"
  },
  "devDependencies": {
    "@ianvs/prettier-plugin-sort-imports": "^4.3.0",
    "prettier": "^3.3.3",
    "prettier-plugin-svelte": "^3.2.5",
    "prettier-plugin-tailwindcss": "^0.6.5"
  },
  "type": "module"
}
